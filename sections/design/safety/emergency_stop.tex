% =============================================================================
% sections/design/safety/emergency_stop.tex  -  Emergency Stop
% Teams: electronics + software (best guess - unconfirmed) / Status: stub
%
% Nested under "Safety Systems" (section-level subsection).
% =============================================================================

\todo[inline]{Description of the emergency stop system: the E-stop button
circuit schematic (jury feedback flagged this as missing in the
Preliminary - it must appear here), the hardware/software architecture
that cuts power/motion on trigger, and the time sequence from trigger to
safe state. Cover all three trigger sources: the system itself, the
operator, and an external actor hitting the emergency button. Include a
reliability argument for the solution, and test results (figures/photos)
if available.}

\begin{figure}[!htbp]
    \centering
    \includegraphics[width=0.9\linewidth, height=0.3\textheight, keepaspectratio]{figures/emergency_stop_scheme}
    \caption{E-Stop button circuit scheme (placeholder)}\label{fig:emergency_stop_scheme}
\end{figure}
