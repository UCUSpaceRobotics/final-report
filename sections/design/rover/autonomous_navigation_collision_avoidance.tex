% =============================================================================
% sections/design/rover/autonomous_navigation_collision_avoidance.tex  -  Autonomous Navigation and Collision Avoidance
% Teams: navigation (best guess - unconfirmed) / Status: stub
%
% Nested under the "Navigational Stack" subsubsection (paragraph level).
% Content required: the data used for collision avoidance, and the
% architecture of Nav2 and the plugins/algorithms used (needs discussion,
% see additional notes). Target length: 2/3 - 1 page. Diagrams required:
% the implemented Nav2 architecture / node diagram (needs discussion, see
% additional notes), and the behavior tree with recovery behaviors.
% =============================================================================

\todo[inline]{Description of the data we are using for collision
avoidance. Architecture of Nav2 and used plugins, algorithms (needs
discussion -- see additional notes). Target length: 2/3 -- 1 page.
Diagrams required: implemented Nav2 architecture / node diagram (needs
discussion -- see additional notes), and the behavior tree with recovery
behaviors.}
