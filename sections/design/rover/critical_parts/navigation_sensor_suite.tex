% =============================================================================
% sections/design/rover/critical_parts/navigation_sensor_suite.tex  -  Navigation Sensor Suite
% Teams: navigation / Status: done
%
% Content required: describe the sensors used for navigation as a bullet
% list with three bullets - Stereo Camera, LiDAR, Wheel Encoders.
% Length: 1/3 - 1/2 page
% =============================================================================

The navigation sensor suite provides near-\ang{360} perception for simultaneous state estimation and collision avoidance.

\begin{itemize}
    \item \textbf{Front stereo vision sensor (ZED 2i):} Mounted at the front of the rover, it provides RGB imagery and depth data at a resolution of \numproduct{1280 x 720}, a frame rate up to \qty{60}{\fps}, a field of view of \ang{110}~$\times$~\ang{70} (H$\times$V) and an effective depth range of \qtyrange{0.3}{20}{\metre}, includes an integrated 6-axis IMU.

    \item \textbf{Top LiDAR (RPLIDAR S2):} Mounted at the top of the rover, it provides \ang{360} 2D scanning with \ang{0.12} angular resolution at \qty{10}{\hertz}, ensures reliable obstacle detection up to \qty{10}{\metre} across varying lighting conditions.

    \item \textbf{Wheel odometry:} Odometry is provided by 14-bit single-turn absolute magnetic encoders on all 8 motors used for the swerve drive.
\end{itemize}

The stereo camera (utilizing depth and IMU data), LiDAR, and encoders provide input for state estimation and localization (\autoref{par:state_estimation_localization}), while the camera and LiDAR also support collision avoidance (\autoref{par:autonomous_navigation_collision_avoidance}). Additionally, the ZED 2i's RGB imagery is used for teleoperation (\autoref{par:operators_camera_suite}).